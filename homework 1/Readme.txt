Jupyter notebook is required for this assignment and subsequent assignments. It is strongly suggested to download and install latest Anaconda(https://www.anaconda.com/distribution/) platform, please set anaconda Python as default path during installing.

Jupyter notebook tutorial: https://realpython.com/jupyter-notebook-introduction/

Packages required(if you cannot find them in your Python environment, just install them):
- gensim(incorporated in Anaconda)
- numpy(incorporated in Anaconda)
- pytorch
- scikit-learn(incorporated in Anaconda)

